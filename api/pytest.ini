[pytest]
testpaths = tests
asyncio_mode = auto
# Fixtures AND tests share one session-scoped event loop: the test engine and
# its asyncpg connections are bound to the loop that created them, so a
# function-scoped loop would try to reuse a connection from another loop.
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
