# Direct runtime dependencies, pinned exactly (review.md R10).
#
# This is the *source* list — the packages the app actually imports. The fully
# resolved tree (these plus every transitive dependency) is locked in
# `requirements.lock`, and that is what CI installs, so a floating transitive
# minor can't turn an unrelated PR red. When you add or bump a package here,
# regenerate the lock (see the header of requirements.lock).
fastapi==0.136.3
uvicorn[standard]==0.48.0
asyncpg==0.31.0
sqlalchemy[asyncio]==2.0.50
pydantic==2.13.4
pydantic-settings==2.14.2
fastapi-users[sqlalchemy]==15.0.5
python-multipart==0.0.32
openai==2.44.0
httpx==0.28.1
openpyxl==3.1.5
pypdf==6.14.2
python-docx==1.2.0
timezonefinder==8.2.4
